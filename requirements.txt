# Capped below 5.0: the dev/prod MariaDB is 10.4.32, and Django 5.0+ requires MariaDB 10.5+.
Django>=4.2,<5.0
mysqlclient>=2.2.8
python-dotenv>=1.2.3
openpyxl>=3.1.5
reportlab>=4.0
matplotlib>=3.8
# Production server (same stack as ControlPresupuestos_AP): waitress is the
# WSGI server, whitenoise serves static files so no IIS/nginx is needed.
waitress>=3.0.2
whitenoise>=6.11.0
